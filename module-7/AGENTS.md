# AI Agent Instructions for module-7

## Project overview
This repository is a small Python exercise for a programming assignment.
- `city_functions.py`: defines the `city_country` helper function and includes top-level example prints.
- `test_cities.py`: contains unit tests using Python's built-in `unittest` framework.

## What agents should know
- Keep changes small and focused on the function behavior and tests.
- The project uses Python, with a simple module and a test file.
- Preferred test command:
  - `python -m unittest discover -s . -p "test_*.py"`

## Recommendations for edits
- Avoid adding unnecessary frameworks or dependencies.
- If converting examples into executable code, use a `if __name__ == "__main__":` guard rather than top-level prints.
- Preserve the simple function signature and expected string formatting.

## Key files
- `city_functions.py` — main function implementation.
- `test_cities.py` — unit test file.
